# Quickstart: Projects

1. Subir dependencias locais conforme README do backend.
2. Rodar migrations:

```bash
npm run migration:run
```

3. Rodar seed de permissoes:

```bash
npm run seed:bootstrap
```

4. Testar endpoints sob `/api/v1/projects` com bearer token de usuario autorizado.

